################################################################################
# Notifiers
################################################################################

# Pushover
notify pushover:
  - name: Pushover
    platform: pushover
    api_key: !secret push_api
    user_key: !secret push_user

# Join
notify join:
  - platform: joaoapps_join
    api_key: !secret join_key
    name: Devices
